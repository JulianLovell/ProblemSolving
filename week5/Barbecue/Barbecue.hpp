#include <vector>
#include <iostream>

using namespace std; 

class Barbecue{
    public:

    int eliminate(int n, vector<int> voter, vector<int> excluded){

        // iterate through excluded and count frequency of each value
        // if tied then
            // iterate through voter and count frequency of each value
            // return 
        // otherwise return most frequent value
    }
};